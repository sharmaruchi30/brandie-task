import 'package:flutter/material.dart';

class ShareAndWin extends StatelessWidget {
  const ShareAndWin({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Share and Win"));
  }
}
